# Handoff — 2026-08-14

If you (or a fresh session) opens ANTZ next, here's where we are.

Three species-page tabs shipped this session, all on one doctrine: real identities and real
envelopes, seeded detail under `core/seed.ts`, everything reconciling by construction. **Eggs**
was rebuilt to Naveen's reference IA (`speciesEggSeason.ts` + rewritten `speciesEggs.tsx`) —
his explicit fourth ask overriding the earlier no-source refusals. **Assessments** was rebuilt
four-section (`speciesAssessData.ts` + `speciesAssess.tsx`, re-exported through
`speciesRegister.tsx`; the old rollup view is gone). **Medical** is net-new as the 11th strip
tab (`speciesMedicalData.ts` + `speciesMedical.tsx`) — mostly REAL row joins since every
medical flow carries animal ids at 100% fill; only recovery durations and the supplements
programme are seeded, and say so. `speciesClinical.tsx` was discovered to be dead code (never
mounted) and is left in place. `npm run build` passes; everything verified in the browser at
1440/1024/390 on Aaru Macaw and under load on Umber Langur. **Nothing is committed — the tree
holds 4 modified + 5 new files, and committing it is still the single most important next
move.** The 1440 tab-strip overflow defect predates this session and now clips harder with 11
tabs.

## Acceptance — try these before trusting the tree

- **Eggs:** open Aaru Macaw → Eggs. You should see four tinted summary cards (Hatched 499 · 60%),
  a stacked Laid›Fertile›Hatched monthly chart, discard pills with "View more (2)", and the
  female table (251 real register females; rows open the animal). If you still see "Young
  recorded" bands, the rewrite didn't land.
- **Assessments:** open Aaru Macaw → Assessments. You should see the Population 315 / Physical
  Health / Endoscopy / Alerts 438 nav with badges. If you see the old "Recorded / When they were
  taken" rollup, `speciesRegister.tsx` isn't re-exporting from `speciesAssess.tsx`.
- **Medical:** the species strip should show a Medical tab (scroll the strip right at 1440 —
  known overflow). Overview should read "14 animals are sick right now" for Aaru Macaw and every
  card/pill/alert should drill to a sheet or an animal page. If the tab is missing, the
  `entity.tsx` wiring (strip entry + mount + import) didn't land.

---

# Handoff — 2026-08-14 (session 2, evening)

If you (or a fresh session) opens ANTZ next, here's where we are.

This session started with a full code review of the uncommitted Eggs/Assessments/Medical work (31
findings filed; the load-bearing ones are recorded below, unfixed). Then **two net-new species tabs
shipped** — **Hospital** (`speciesHospital.tsx` + `speciesHospitalData.ts`) and **Lab**
(`speciesLab.tsx` + `speciesLabData.ts`) — taking the strip to 13. Both follow the Eggs doctrine:
real spine, seeded detail, declared. The Lab one deliberately overrides the documented `NoSource`
deletion of the lab module and is the ONLY tab correctly wired to the global date/site scope. The
rest of the session was ~60 UI feedback items, most of them one instruction: strip every qualifier
beside or beneath a heading, which was fixed **structurally** in `Band` rather than at 30 call
sites. Finally, the chart tooltip was rebuilt: one `ChartTip`/`useChartTip` in `exec/system.tsx`,
portalled to `body`, replacing three different mechanisms, with a press-to-read model on touch.
`npm run build` passes. **Nothing is committed — the tree now holds 14 modified + 15 new files,
and a second interactive session (`antz-command-centre-v5-c9`) has been editing the same tree in
parallel (it owns `speciesHeader.tsx`, `speciesRange.tsx`, `public/world.svg` and the
`_planning/species-*.html` studies). Agree a commit boundary with that session before committing —
that is the single most important next move.**

## Known defects, filed and NOT fixed

- **Medical, Hospital and Eggs ignore `scope.win`.** They report all-time figures under a header
  stating a date window. Lab is the only tab that cuts at the row. This is the highest-severity
  finding from the review.
- `speciesAssessData.ts` — under a site pill, `perAnimal` divides a NAME-WIDE row count by a
  site-scoped target, inflating every animal's record count and emptying the Under-Monitored alert.
- Medical's "Overdue animals" counts dose ROWS, not animals, while Insights counts distinct animals
  for the same programme.
- Removing the pagers means Eggs/Assessments tables render every row; fine at 31 females, heavy on a
  several-thousand-animal species. `VirtualRows` in `perf.tsx` is the existing escape hatch.

## Acceptance — try these before trusting the tree

- **Hospital:** open any species → Hospital. Dark-teal strip of three large numbers (Repeatedly
  Hospitalised / Died in Care / Long Stay), then In care now · Admissions · Avg stay, then
  Admissions Trend beside an Outcomes donut, then Admissions by Hospital beside Length of Stay,
  then Surgery. If the tab is missing, the `entity.tsx` wiring didn't land.
- **Lab:** open → Lab. A "Lab" toolbar with the date + site pills at top right; change the date and
  EVERY figure must move (all-time vs Last 30 days differ sharply). If figures don't move, the tab
  is not reading `scope.win`.
- **Tooltips:** hover any bar, donut segment or distribution bar — one dark tip appears directly
  above the pointer, not floating above the chart. On a phone, press and hold a column; lifting
  clears it. If a tip appears at the top of the plot regardless of the bar, the old per-chart
  tooltip is still in place.
- **The strip pass:** no band on any species tab should show grey qualifier text beside its title,
  and no stat cell should have a third line. If one does, it is a ReactNode aside (a control) or a
  component that still renders its own sub-line.

---

# Handoff — 2026-08-14, second session (species header · tabs · native range map)

The species page's chrome was settled end-to-end this session, all uncommitted on NNV5.
**Header**: five structural studies → five colour studies → the final `speciesHeader.tsx` — one
unified Emerald light-on-dark surface (`#3e7d5d→#2f6449→#245239`), no borders, no black ink,
rounded-square glass back button, and NO date/site filter pills (Naveen's explicit direction —
figures still cut to the URL scope). **Tabs**: `EntityTabs` is now studies-variant 1 — 10px rail,
8px tabs, active fill the header's emerald, and one pill that slides between tabs (300ms house
curve). **Native range** (Profile tab): pills became a numbered list beside an interactive map
(`speciesRange.tsx` + lazy `public/world.svg`) — rests at world view, 380ms dwell glides to a
place, click locks, the place's name label anchors to its country. Study decks live in
`_planning/species-{header,tabs,range-map}.html`. The most important next move: commit this work —
it is sitting in a tree shared with another active session's in-progress files.

Acceptance checks, per fix:
- **Header:** open any species page. You should see one dark emerald header card with the name
  once, class under it, IUCN/CITES glass pills, five stats inside the same surface — and NO
  "This month"/"All sites" pills anywhere in it. If you see a light green gradient card or filter
  pills, the old `SpeciesHeader` is being served — hard-refresh; HMR from parallel sessions
  leaves stale mounts.
- **Tabs:** click between tabs. The emerald pill should SLIDE to the new tab (~300ms), text and
  icon colours cross-fading with it; corners are 8px, never full-round. If the selection jumps
  instantly, check `prefers-reduced-motion` (which disables the slide on purpose) before
  suspecting the code.
- **Range map:** open Profile on a wide-range species (Cinder Squirrel). You should see a
  numbered list of every place beside a world map with the range tinted. Rest ~0.4s on a row —
  the map glides into that country and its NAME LABEL appears anchored on it; click the row to
  lock, click open water to release. If the map area is blank, `/world.svg` 404'd (it must sit in
  `public/`); the list stands alone and says so.

Pending: `npm run build` currently FAILS on `speciesLabData.ts` (the other session's in-flight
import of `SPECIES` from `core/store`) — every file this session touched type-checks clean via
`tsc -p tsconfig.app.json`. Known data quirk surfaced but unfixed: `native_countries` names
containing commas split into wrong list entries on the Profile tab.

---

# Handoff — 2026-08-14 (session 4, species list)

If you (or a fresh session) opens ANTZ next, here's where we are.

The **Species List** shipped at the existing `#/browse/species` route (`src/v4/speciesList.tsx` +
`speciesListData.ts`; `App.tsx` special-cases the species kind, the other thirteen keep
`EntityBrowser`) — five totals, a nine-group faceted panel, an eleven-column table with the NO and
SPECIES columns pinned, sorting, search over common + scientific name, client-side CSV of the
filtered rows, and rows that open the existing species page with its thirteen tabs untouched. Then
Naveen directed that **every species count in the product be a link whose destination arrives
pre-filtered**, which needed `scope.tsx` to stop stripping page-owned query params — it now merges
non-scope keys and the scope still wins on `w`/`s`/`from`/`to`. Three counts are wired (Home's new
`SpeciesCount` card — he lifted the "don't change Home" rule for it — the Animal Population hero
stat, and the site page's "Species held"), and the hero was corrected from 4,745 to **2,411**
because it was counting registry pairs, not names. `npm run build` passes and nineteen routes
smoke-tested clean. **Nothing is committed. The tree is shared with three other live sessions —
`entity.tsx` shows ~300 changed lines of which 8 are this session's — so agree a commit boundary
before committing; that is still the single most important next move.**

## Acceptance — try these before trusting the tree

- **The list:** open `#/browse/species`. 2,411 species / 110,020 animals across five tinted cards,
  Category and Class open in the panel, eleven columns with a horizontal scroll and the species
  name staying put as you scroll right. If Sexed % and Chip % are missing, an old container-query
  hide is still in the file.
- **The filtered link:** open a site page (`#/e/site/ironwood-refuge`) and click **Species held
  517**. You should land on the list reading *517 species · 7,661 animals*, breadcrumb "Overall ›
  Ironwood Refuge", every row Sites = 1, and Class recounted to Aves 425. If it reads 2,411, the
  `scope.tsx` param-preservation didn't land.
- **Direct filter URLs:** `#/browse/species?cls=Aves&iucn=EN` must open on 49 rows with both boxes
  already ticked. Tick a third box and the address bar must update without adding a history entry.
- **The counts agree:** Home's card, the Animal Population hero and the list must all read 2,411.
  A 4,745 anywhere means something is still counting `<site>:<name>` ids.

## Left deliberately inert, with reasons

Enclosure-page species count (no enclosure facet), the preventive/medical/mortality flow sheets
(no "had a vaccination this window" facet), `panels.tsx`'s drill sheet (leaving a sheet needs
`drillTo`'s history unwind), `records.tsx` (hardcoded strings), `conservationPanel.tsx` (dead code
on an authored species list that isn't in `core/world.ts`). Linking any of these means adding the
matching facet to the list first.

---

# Handoff — 2026-08-14, evening session (Home background & foliage)

If you (or a fresh session) picks this up next, here is the state.

This session was entirely the **Home page background**, and it took five wrong turns before landing,
so read the "foliage" paragraph in the design-preferences memory before touching it again. What
shipped: the ground descent in `.page-ground.is-home` now runs the **full page height** (pixel stops
above the hero hand-over so they stay locked to `--env-top`, percentage stops below it) instead of
dying to flat sage by 1600px; the vector `Landscape` layer is **unmounted on every route** in
`Shell.tsx`; and the hero's botanical frame is now a separate strip, `HeroFoliage` in `Home.tsx` /
`.hero-foliage` in `index.css`, **pinned to y=0** so the leaves bleed off the top of the screen the
way the illustrator drew them. Nothing is drawn below the hero. Four rejected directions are
preserved as `_planning/home-background.html`, `home-foliage.html` and `home-hero-continuity.html`.

**Verify it like this.** Open `#/` at 500px and at 1000px. You should see monstera leaves, hanging
vines and pink wisteria at the top-left and top-right, cut by the *screen's* top edge; the scene
(elephants, pond) sitting below the total as it always has; and clean shade from EXECUTIVE KPIS
downward with no drawn marks at all. If you see a horizontal seam slicing through leaves partway
down the page, the artwork cap in `ForestBand` has gone back above `34cqw`. If the page fades to
near-white by the Trends section, the percentage stops in `.page-ground.is-home` were lost.

**Blocking:** `npm run build` does **not** pass. `src/exec/system.tsx:792` has a type error in an
in-flight edit that is not from this session — `const Cell = (s.href ? 'a' : 'span') as 'span'`
cannot take an `href`; the working sibling at line ~2094 uses `as 'a'`. The Vite dev server on 5202
renders fine because it does not type-check, which is where the Home work was verified. Fix that
cast before trusting a build. The Home design itself has not had an explicit sign-off yet — the last
instruction was a rework, which was delivered but not confirmed.

## Addendum — where the species-list work actually is

All of the above shipped and is on `origin/NNV5`, but **not under a commit that names it**. A
parallel session ran `git add -A` on the shared tree and pushed everything as `bf8eb49` —
*"Home: carry the ground the whole page, and pin the hero's foliage to y=0"* — with the species
list, `scope.tsx`, the header, the range map and the four species tabs folded in under one
trailing "also in this commit" line. So `git log --oneline` will not show that this branch gained
a species list; `git show --stat bf8eb49` will. Local HEAD and the remote both read
`bf8eb491ad136f28174259d904d14423171128a0`, verified against `git ls-remote`, and the tree is
clean.

Open decision, deliberately not taken: whether to `git commit --amend` the message and
`--force-with-lease`. Safe only if none of the peer sessions has pulled `bf8eb49`; ask them first.
The alternative is to leave it and rely on this note.

## Correction — the build blocker above is stale

The "**Blocking:** `npm run build` does not pass" paragraph in the evening-session section was true
when written and was resolved before anything was committed. The `as 'span'` cast at
`src/exec/system.tsx:792` was fixed in-flight; `bf8eb49` was built clean (`✓ built in 1.84s`) and
staged only after that. Do not go looking for it. Everything else in that section stands, including
that the Home background/foliage design has still not had an explicit sign-off.
